
export interface NavProps {
    mobNav: boolean;
    setmobNav: (p: boolean) => void;
}

export interface MobNavProps {
    mobNav: boolean;
    setmobNav: (p: boolean) => void;
}

export interface HomeProps {
    mobNav: boolean;
    setmobNav: (p: boolean) => void;
}

export interface modals{
    locations: boolean,
    services: boolean,
    resources: boolean
}

